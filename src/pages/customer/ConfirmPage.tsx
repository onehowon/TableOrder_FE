// src/pages/customer/ConfirmPage.tsx
export default function ConfirmPage() {
    // cart 컨텍스트에서 아이템 꺼내서, postCustomerOrder 호출 → orderId 받고 navigate(`/customer/${tableId}/orders?orderId=${id}`)
    return <div>주문 확인 화면</div>
  }